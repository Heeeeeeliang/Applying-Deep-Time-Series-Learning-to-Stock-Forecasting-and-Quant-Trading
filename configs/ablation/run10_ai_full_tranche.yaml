# Ablation run 10
# Vol + Trend Bypass + Turning Point + Direction + Signal Reversal + Vol Collapse + Tranche Exit

run_number: 10
is_final: false
config: 20260316_140416_ai_full_tranche
display_name: Vol + Trend Bypass + Turning Point + Direction + Signal Reversal + Vol Collapse + Tranche Exit
description: AI full with tranche/trail exits
fee_structure:
  commission: 0.001
  slippage: 0.0005
  min_commission: 1.0
ablation:
  enable_tranches: true
  enable_signal_reversal: true
  enable_vol_collapse: true
  enable_preemption: true
  enable_trail_stop: false
overrides:
  strategy:
    dynamic_execution: true
