# Notebook cleanup — outstanding items

Generated by `tools/clean_notebooks.py` on the first pass. These are findings
that the script flagged but does not auto-fix because they need a human
judgment call.

## Hard constraint violations to fix before publishing

### `torch.load` without `weights_only=True` (4 occurrences)

`04_backtest_and_ablation/01_end_to_end_pipeline.ipynb` cells 1, 4, 9, 10.

The constraint in the project brief says every `torch.load(...)` example must
pass `weights_only=True`. Replace each call:

```python
# before
state = torch.load(ckpt_path)
# after
state = torch.load(ckpt_path, weights_only=True)
```

If the checkpoint contains arbitrary Python objects (optimizer state, custom
classes), split it into a state-dict and a separate metadata JSON; do not
ship a checkpoint that requires unsafe load.

### Remaining absolute path literals (2 occurrences)

- `01_direct_prediction_ceiling/01_nine_method_ceiling.ipynb` cell 4
- `01_direct_prediction_ceiling/02_missing_metrics.ipynb` cell 1

These are Windows `E:\Uni\...` paths that the regex did not catch because
they are inside string concatenations or f-strings rather than plain
literals. Replace with `CONFIG['data_dir'] / ...`.

### Long commented-out block (1 occurrence)

`03_turning_point_layer/01_multiscale_cnn.ipynb` cell 4 has a commented block
> 5 lines. Either delete it or move it into the chapter's notebook commentary
as a `markdown` cell.

## Manual review still recommended

- For every notebook, open the markdown header cell that the script
  injected and confirm the `runtime` and `gpu` estimates are still accurate.
- Run each notebook top-to-bottom on a fresh clone with the published data —
  the script does **not** verify that the cleaned notebooks execute. Phase 4
  of the cleanup contract requires this.
- Re-run `python tools/clean_notebooks.py --verbose` after fixing the items
  above; it should report zero warnings.
